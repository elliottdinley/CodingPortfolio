def longest_word(sentence):
    count = 0
    longest = 0
    for x in sentence:
        if x != " ":
            count += 1
            if count > longest:
               longest = count
        else:
            count = 0
    return longest

print(longest_word(input()))
